import 'package:flutter/material.dart';

class SewingFormScreen extends StatelessWidget {
  const SewingFormScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Sewing form')));
}
