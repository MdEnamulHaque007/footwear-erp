import 'package:flutter/material.dart';

class ColorPalette {
  static const primary = Color(0xFF1976D2);
  static const secondary = Color(0xFFFF6F00);
  static const success = Color(0xFF2E7D32);
  static const error = Color(0xFFC62828);
  static const warning = Color(0xFFF57C00);
  static const info = Color(0xFF0277BD);
  static const canvas = Color(0xFFF5F5F5);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF212121);
  static const muted = Color(0xFF757575);

  static const dashboard = primary;
  static const masterLc = Color(0xFF7B1FA2);
  static const purchaseOrder = Color(0xFF388E3C);
  static const cutting = Color(0xFFF57C00);
  static const sewing = Color(0xFFC2185B);
  static const production = Color(0xFF0288D1);
  static const issue = Color(0xFF5D4037);
  static const export = Color(0xFF00796B);
  static const admin = Color(0xFFD32F2F);
  static const reports = Color(0xFF455A64);

  // Legacy aliases used by existing screens.
  static const ink = textPrimary;
  static const teal = primary;
  static const coral = error;
}
