import 'package:flutter/material.dart';

class ReportPreview extends StatelessWidget {
  const ReportPreview({super.key});
  @override
  Widget build(BuildContext context) => const Text('Report preview');
}
