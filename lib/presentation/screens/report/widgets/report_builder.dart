import 'package:flutter/material.dart';

class ReportBuilder extends StatelessWidget {
  const ReportBuilder({super.key});
  @override
  Widget build(BuildContext context) => const Text('Report builder');
}
