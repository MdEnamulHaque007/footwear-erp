import 'package:flutter/material.dart';

class AvailableSewingList extends StatelessWidget {
  const AvailableSewingList({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
