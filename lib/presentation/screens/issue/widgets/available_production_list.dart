import 'package:flutter/material.dart';

class AvailableProductionList extends StatelessWidget {
  const AvailableProductionList({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
