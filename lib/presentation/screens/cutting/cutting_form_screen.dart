import 'package:flutter/material.dart';

class CuttingFormScreen extends StatelessWidget {
  const CuttingFormScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Cutting form')));
}
