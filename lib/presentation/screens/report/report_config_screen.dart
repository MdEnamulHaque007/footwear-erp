import 'package:flutter/material.dart';

class ReportConfigScreen extends StatelessWidget {
  const ReportConfigScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Report configuration')));
}
