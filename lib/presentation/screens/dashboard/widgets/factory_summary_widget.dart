import 'package:flutter/material.dart';

class FactorySummaryWidget extends StatelessWidget {
  const FactorySummaryWidget({super.key});
  @override
  Widget build(BuildContext context) => const Text('Factory summary');
}
