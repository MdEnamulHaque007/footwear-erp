import 'package:flutter/material.dart';

class AvailablePOList extends StatelessWidget {
  const AvailablePOList({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
