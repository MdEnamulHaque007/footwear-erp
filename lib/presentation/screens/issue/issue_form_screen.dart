import 'package:flutter/material.dart';

class IssueFormScreen extends StatelessWidget {
  const IssueFormScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Issue form')));
}
